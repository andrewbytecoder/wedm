## Wails build

This application shell uses **Wails v2** with a **Vue 3 + TypeScript** frontend under `frontend/`.

## Prior release notes

See the original project changelog in the upstream repository history.
